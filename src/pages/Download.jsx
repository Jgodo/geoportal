import React from 'react';

const styleDownload = {
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center',
    height: '90vh',
};

export default function Download() {
    return (
        <div style={styleDownload}>
            <h1>Download</h1>
        </div>
    );
}