import React from 'react';

const styleSurvey = {
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center',
    height: '90vh',
};

export default function Survey() {
    return (
        <div style={styleSurvey}>
            <h1>Survey</h1>
        </div>
    );
}